<!DOCTYPE html>
<html>
	<head>
		<meta charset = "utf-8">
		<title>Réinitialisation de mot de passe</title>
	</head>

	<body>
		<form action="{{ path('ts_nao_reset_password') }}", method="post">
			<input type="hidden" name="email" id="email" value="{{ email }}">

			{{ form_rest(form) }}

		</form>
	</body>
</html>