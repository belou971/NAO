<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8" />
		<title>Liste des demandes d'amélioration de compte</title>
	</head>

	<body>
		<p>
			<a href="{{ path('ts_nao_dashboard') }}">Retour</a>
		</p>

		{% for request in requestList %}
			<p>
				{{ request.name }} {{ request.surname }} - {{ request.email }} <br />
				<a href="{{ asset('uploads/grades/') }}{{ request.grade }}" target="_blank">Document fournis</a>
			</p>
		{% else %}
			<p>Aucune nouvelle demande d'amélioration.</p>
		{% endfor %}
	</body>
</html>